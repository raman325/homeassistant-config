{
    ir_send:
    {
        url: !secret url_ir_send
        method: POST
    }
    kodi_send:
    {
        url: !secret url_kodi_send
        method: POST
        payload: '{"kodiid":"{{ data.kodiid }}","token":"{{ data.token }}"}'
        content_type:  'application/json'
    }
}